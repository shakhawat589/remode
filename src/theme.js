import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f59542",
      light: "#f5b942",
    },
    secondary: {
      main: "#15c630",
    },
    gray: {
      main: "#999",
    },
  },
});